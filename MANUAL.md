# Folumi 使用手册

> 适用版本：0.4.2

Folumi 是一个本地优先的个人知识助手。界面按照用户目的把长期信息分成三个边界清晰的领域：

- **资料（Sources）**：外部只读材料，供检索、阅读和引用；
- **笔记（Notebook）**：用户拥有的 Markdown 内容，可直接编辑和导出；
- **个性化（Personalization）**：管理助手长期了解的个人信息、过往对话引用权限与助手设定。

产品只有一个助手，不需要创建或选择 Tutor，也没有 Quiz、Space、Student Profile 或独立 Research 模式。

## 1. 首次使用

1. 打开“设置 > LLM”，添加一个可用的模型配置；
2. 如需文档语义检索，在“设置 > 嵌入模型”配置 embedding；
3. 进入“资料”创建资料集，并导入用于 RAG 检索的文档；
4. 进入“笔记”记录、管理或查看 Markdown 笔记；
5. 返回“助手”，直接提问，或从输入框关联指定资料集和笔记；
6. 在回答的引用中打开原始资料，确认答案依据。

首次引导只介绍模型、资料和提问三个必要步骤。以后可在“设置 > 帮助”随时重新查看，不会修改业务数据或重置会话。

## 2. 助手

### 2.1 普通对话

助手是唯一的对话入口。它可以根据任务按需使用知识检索、网页搜索、网页读取、代码执行和笔记工具。普通 Chat 同时承担问答、查证和资料调研，不再提供单独的 Research 模式或任务动作。

每个会话会保存消息、引用、工具轨迹和必要的结构化结果。切换会话或重新打开应用后，可继续查看原有内容。

### 2.2 指定资料范围

输入框允许选择一个资料集，并通过笔记选择器引用具体 Note。引用笔记时，系统只保存稳定标识和显示信息；agent 需要内容时会调用 `read_notebook_item` 精确读取，不会把所有笔记自动塞入 prompt。

未选择资料集时，助手不会自动搜索任意资料集。界面会持续显示当前会话的资料范围，避免用户误以为回答使用了未授权资料。

### 2.3 停止与恢复

生成过程中可以停止任务或切换到其他会话。后台任务状态和已完成结果会回到原会话；恢复逻辑以持久化 session 与 runtime trace 为准，不依赖页面临时状态。

## 3. 资料与笔记

资料与笔记是两个独立的一级工作区。资料页只保存用于 RAG 的只读外部来源，并以“资料集”分组；笔记页面用于记录、管理和查看用户拥有的 Markdown 内容。代码和 API 内部仍沿用 Knowledge Base 与 Notebook 名称。

### 3.1 Sources

Sources 是外部事实材料。支持导入、处理状态、失败原因、重试、重复检测、重建索引、搜索、预览和原文定位。向量索引与 chunks 是可重建派生物，原始资料才是权威数据。

回答引用知识时，只有通过 runtime Evidence 验证的读取结果才能成为可信引用。点击引用可回到对应来源与位置。

### 3.2 独立笔记页面

Notes 是 Markdown 文件，可在应用数据目录中管理，也可绑定外部 Vault。支持目录、标签、Wiki 链接、反向链接、导入、导出和搜索。

笔记页面采用编辑器式三栏布局：左侧“资源管理器”按真实目录展示笔记，可在根目录或指定目录中新建笔记和子目录。新建目录会直接在文件树中输入名称，Enter 或失焦确认、Esc 取消；桌面版还可以在文件树空白处右键，从根目录新建笔记或目录。笔记右键菜单支持打开、复制路径和删除；目录右键菜单支持在目录内新建、复制路径和删除。删除非空目录时会显示确认弹窗，列出其中的笔记、其他文件和子目录数量；确认后目录会先移入 Folumi 恢复区，界面提供撤销。目录展开状态会在本地保存，大量笔记使用虚拟滚动，标题、路径和标签可直接筛选。中间区域用于 Markdown 编辑与渲染预览；右侧“关联信息”可折叠，并显示局部关系图、未解析 Wiki 链接、标签、出链和反向链接。点击已解析链接会打开目标笔记，点击未解析链接可以创建对应笔记。页面顶部会显示 Vault 文件监听状态，也可以手动刷新。

助手对笔记的写入使用受限工具：路径必须位于 Vault 根目录内，更新基于 revision，删除需要确认并保留恢复路径。Sources 不会被笔记工具改写。

## 4. 个性化与对话连续性

### 4.1 用户控制

个性化页面默认打开“关于我”选项卡。“使用个人信息”默认关闭；开启后，用户可以新增、搜索、编辑、置顶、完成、重新确认和彻底移除个人信息。助手也可以主动识别你直接陈述的姓名、稳定偏好和长期目标等持久信息；默认每次新增都会请求审批。开启“允许助手自动补充”后，助手新增可以直接保存，但移除仍需审批，敏感、推断和临时信息仍不应被主动保存。编辑时可以调整或清除有效期；过期条目需要选择新的未来日期或清除有效期后再确认。旧版分层记忆与后台整理链路已经退役，笔记和资料操作不会在后台被采集为个人信息。

### 4.2 个人信息与参考过往对话

- 个人信息（内部 Saved Memory）保存用户明确添加，或助手依据用户直接陈述并按当前审批权限写入的事实、偏好、目标和连续性事项；
- “参考过往对话”（内部 History Recall）是默认关闭的独立能力。先开启“使用个人信息”，再开启该选项后，只有当用户明确询问或引用旧对话时，助手才可通过可见的 runtime Knowledge 工具按需搜索符合权限和生命周期条件的旧 Sessions；产品不挂载 run 前自动召回插件，检索结果也不会自动形成个人信息；
- 当前实现使用 runtime 的持久 SQLite/FTS5 派生索引；应用启动时按 revision 增量对账，删除或更新 Session 会通过 runtime observer 使派生结果失效，Session 仍是唯一权威数据；
- 历史搜索和读取会出现在工具 trace 中，读取来源可跳转到对应会话；Phase 2 离线召回基线已经通过，指标和复现方法记录在 `docs/qa/history-recall-acceptance.md`；
- 每条 Saved Memory 是独立可见、带生命周期的全局产品条目，不再使用 L1/L2/L3 文件层次；
- 同一主题的冲突条目会在确认后替代旧条目；完成、已替代和过期内容不参与正常召回；
- 当前会话内容仍属于 runtime Session。发送第一条消息前可开启“临时对话”；该 Session 会标记为 runtime Temporary，不挂载 Saved Memory 或 History Recall，也不会进入历史检索索引。临时会话仍会显示在最近会话中，便于用户继续查看或主动删除；
- Session 负责会话内连续性，也可以作为 Saved Memory 的来源，但不是 Saved Memory 的另一种作用域。

开启“使用个人信息”后，新一轮助手运行可以按需检索有效条目。助手可以主动提出保存明确、持久且有后续价值的信息；默认界面会展示最终内容并要求逐次确认，拒绝后不会写入。用户授予自动补充权限后，只有新增写入可以跳过该确认；移除、冲突处理和彻底删除不跳过。直接在个性化页面操作同样遵守冲突、revision 和彻底移除规则。

“导出”会生成 `folumi-memory-export.json`，包含当前 Memory 设置和仍然存在的所有状态条目（包括已完成、已替代和已过期条目）。导出不会包含内部版本历史、tombstone、策略密钥或已经彻底遗忘的正文；该文件当前用于审阅和备份，不作为旧记忆迁移或静默恢复入口。

详细设计与后续阶段见 `docs/specs/2026-08-03-user-memory-redesign.md`。

### 4.3 助手设定

切换到个性化页面的“助手设定”选项卡，可以管理单一助手的名称和行为说明。这些设置会自动保存，并用于之后创建的新会话；行为说明不能覆盖安全规则、工具审批或数据权限。设置页不再提供重复的助手设定入口。

## 5. 设置与本地数据

设置包括外观、LLM、embedding、网页搜索、Notebook 存储、能力治理和帮助。助手名称、行为说明、个人信息与相关权限位于独立的“个性化”页面。API Key 当前保存在本地 `settings.json`，请勿提交或共享应用数据目录。

源码运行默认使用：

```text
<repo>/.llm-tutor/
```

也可覆盖数据目录：

```powershell
$env:LLM_TUTOR_HOME="D:\FolumiData"
cargo run -p tutor-web -- --data-dir "D:\FolumiData"
```

桌面版由 Tauri 传入操作系统应用数据目录。当前保留旧目录键是为了让已有数据保持可发现，不代表产品仍保留旧教学模型。Folumi 不提供旧 Tutor/Quiz 数据的应用内迁移或归档能力，也不会主动删除遗留文件；如需保留，请直接备份整个应用数据目录。

## 6. 故障排查

- 模型不可选：检查“设置 > LLM”中的服务地址、模型名和 API Key；
- 文档无法检索：检查导入状态与 embedding 配置，必要时重试或重建索引；
- 引用打不开：确认原始文件仍存在，并在资料页中重新处理来源；
- 外部 Vault 不更新：确认目录权限后手动刷新；文件监听状态会显示最近刷新结果；
- 不希望使用个人信息：关闭“使用个人信息”即可让新 run 不挂载 Saved Memory 或 History Recall；已有条目仍保留并可在页面中逐项彻底移除。
- 只希望隔离某次对话：在发送第一条消息前开启“临时对话”。会话创建后不能切换该属性；它不会读取或写入 Saved Memory，也不会检索旧会话或进入未来的历史检索结果。

## 7. 当前边界

临时附件仍等待 `llm-harness-runtime` 提供一等 `ContextAttachment` 契约。在此之前，Folumi 不建设平行上下文注入系统；普通会话附件和持久 Sources/Notes 继续使用各自明确的现有边界。
