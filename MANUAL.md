# Folumi 使用手册

Folumi 是一个本地优先的个人知识助手。它把长期资料分成三个边界清晰的领域：

- **Sources**：只读的原始资料，供检索、阅读和引用；
- **Notes**：用户拥有的 Markdown 笔记，可直接编辑和导出；
- **Memory**：规划为用户确认的长期条目与可选历史检索；当前助手配置可用，长期记忆实现尚未重新接入。

产品只有一个助手，不需要创建或选择 Tutor，也没有 Quiz、Space、Student Profile 或独立 Research 模式。

## 1. 首次使用

1. 打开“设置 > LLM”，添加一个可用的模型配置；
2. 如需文档语义检索，在“设置 > 嵌入模型”配置 embedding；
3. 进入“知识库”导入用于 RAG 检索的资料；
4. 进入“笔记”记录、管理或查看 Markdown 笔记；
5. 返回“助手”，直接提问，或从输入框关联指定知识库和笔记；
6. 在回答的引用中打开原始资料，确认答案依据。

首次引导只介绍模型、知识库和提问三个必要步骤。以后可在“设置 > 帮助”随时重新查看，不会修改业务数据或重置会话。

## 2. 助手

### 2.1 普通对话

助手是唯一的对话入口。它可以根据任务按需使用知识检索、网页搜索、网页读取、代码执行和笔记工具；Research 是聊天中的任务动作，而不是另一套会话模式。

每个会话会保存消息、引用、工具轨迹和必要的结构化结果。切换会话或重新打开应用后，可继续查看原有内容。

### 2.2 指定资料范围

输入框允许选择一个知识库，并通过笔记选择器引用具体 Note。引用笔记时，系统只保存稳定标识和显示信息；agent 需要内容时会调用 `read_notebook_item` 精确读取，不会把所有笔记自动塞入 prompt。

未选择知识库时，助手不会自动搜索任意知识库。界面会持续显示当前会话的资料范围，避免用户误以为回答使用了未授权资料。

### 2.3 Research 任务

需要系统检索和长篇综合时，可点击 Research 动作。助手会执行受控研究流程，并把带来源的报告附着到原会话。报告仍属于一次助手任务，不建立独立 Research 产品层级。

### 2.4 停止与恢复

生成过程中可以停止任务或切换到其他会话。后台任务状态和已完成结果会回到原会话；恢复逻辑以持久化 session 与 runtime trace 为准，不依赖页面临时状态。

## 3. 知识库与笔记

知识库与笔记是两个独立的一级工作区。知识库只保存用于 RAG 的只读来源；笔记页面用于记录、管理和查看用户拥有的 Markdown 内容。

### 3.1 Sources

Sources 是外部事实材料。支持导入、处理状态、失败原因、重试、重复检测、重建索引、搜索、预览和原文定位。向量索引与 chunks 是可重建派生物，原始资料才是权威数据。

回答引用知识时，只有通过 runtime Evidence 验证的读取结果才能成为可信引用。点击引用可回到对应来源与位置。

### 3.2 独立笔记页面

Notes 是 Markdown 文件，可在应用数据目录中管理，也可绑定外部 Vault。支持目录、标签、Wiki 链接、反向链接、导入、导出和搜索。

助手对笔记的写入使用受限工具：路径必须位于 Vault 根目录内，更新基于 revision，删除需要确认并保留恢复路径。Sources 不会被笔记工具改写。

## 4. Memory 与 Continuity

### 4.1 用户控制

Memory 页面默认打开“长期记忆”选项卡。当前旧版分层记忆与后台整理链路已经退役，修订方案方向已经确认但尚未实施，因此该选项卡仅显示过渡说明，暂不召回、写入或维护长期记忆。Assistant 对话、Notebook 操作和知识库操作都不会在后台被采集为记忆。

### 4.2 新方案方向（尚未启用）

- Saved Memory 只保存用户明确要求或确认的事实、偏好、目标和连续性事项；
- History Recall 是默认关闭的独立能力，只读检索 runtime Sessions，不会自动形成 Saved Memory；
- 每条 Saved Memory 是独立可见、带作用域和生命周期的产品条目，不再使用 L1/L2/L3 文件层次；
- 同一作用域的冲突条目会在确认后替代旧条目；完成、已替代和过期内容不参与正常召回；
- 当前会话内容仍属于 runtime Session；临时对话不读写记忆，也不进入历史检索索引；
- 第一阶段只启用全局作用域，逻辑工作区实体另行定义后才启用 workspace 作用域。

详细提案见 `docs/specs/2026-08-03-user-memory-redesign.md`，实施和验收完成前不会接入运行链路。

### 4.3 助手配置

切换到记忆页面的“助手配置”选项卡，可以管理单一助手的名称和行为说明。这些设置会自动保存，并用于之后创建的新会话；行为说明不能覆盖安全规则、工具审批或数据权限。设置页不再提供重复的助手配置入口。

## 5. 设置与本地数据

设置包括外观、LLM、embedding、网页搜索、Notebook 存储、能力治理和帮助。助手名称、行为说明、记忆内容与总开关位于独立的“记忆”页面。API Key 当前保存在本地 `settings.json`，请勿提交或共享应用数据目录。

源码运行默认使用：

```text
<repo>/.llm-tutor/
```

也可覆盖数据目录：

```powershell
$env:LLM_TUTOR_HOME="D:\FolumiData"
cargo run -p tutor-web -- --data-dir "D:\FolumiData"
```

桌面版由 Tauri 传入操作系统应用数据目录。当前保留旧目录键是为了让已有数据保持可发现，不代表产品仍保留旧教学模型。Folumi 不提供旧 Tutor/Quiz 数据的应用内迁移或归档能力，也不会主动删除遗留文件；如需保留，请直接备份整个应用数据目录。

## 6. 故障排查

- 模型不可选：检查“设置 > LLM”中的服务地址、模型名和 API Key；
- 文档无法检索：检查导入状态与 embedding 配置，必要时重试或重建索引；
- 引用打不开：确认原始文件仍存在，并在知识库中重新处理来源；
- 外部 Vault 不更新：确认目录权限后手动刷新；文件监听状态会显示最近刷新结果；
- 不希望使用记忆：当前长期记忆尚未启用；新方案实施后可关闭 Memory 总开关、单独关闭历史检索，并按需遗忘已有条目。

## 7. 当前边界

临时附件仍等待 `llm-harness-runtime` 提供一等 `ContextAttachment` 契约。在此之前，Folumi 不建设平行上下文注入系统；普通会话附件和持久 Sources/Notes 继续使用各自明确的现有边界。
